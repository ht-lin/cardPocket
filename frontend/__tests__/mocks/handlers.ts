import { http, HttpResponse } from 'msw';

export const handlers = [
  // Auth handlers — filled in FE-AUTH
  // http.post('/api/auth/login', () => HttpResponse.json({ ... })),

  // Cards handlers — filled in FE-CARD
  // http.get('/api/cards', () => HttpResponse.json([])),
];
